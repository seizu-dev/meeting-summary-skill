---
name: meeting-summary
description: ローカルの録音ファイル（音声・動画）をffmpegでmp3に変換してGemini Notebookに渡し、事前情報と合わせて要約させてミーティングノートを作成する
trigger: /meeting-summary
model: opus
effort: high
---

# /meeting-summary

ローカルに保存された会議の録音ファイルを Gemini Notebook（2026年7月16日に NotebookLM から改名）に渡して文字起こし・要約させ、Markdown のミーティングノートを作成する。

自動文字起こしが使えない会議ツール（無料プランに落ちた tl;dv、OBS や IC レコーダーでの自前録音、Zoom のローカル録画など）で録音だけが手元に残っているケースを想定している。

引数には録音ファイルのパスを渡す。省略された場合は Step 1 の手順で候補を列挙してユーザーに選んでもらう。

## 前提条件

| 項目 | 内容 |
| --- | --- |
| ffmpeg | 必須。録音ファイルを mp3 に変換するために使う |
| Gemini Notebook MCP | 必須。文字起こしと要約を Gemini Notebook に任せる |
| アカウント上限 | Gemini Notebook は notebook 100件 / 1 notebook あたり source 50件。使い捨て運用なら定期的に整理が必要 |

MCP ツール名は接続方法によってプレフィクスが変わる。claude.ai のコネクタ経由なら `mcp__claude_ai_notebooklm__<tool>` になる。**ツール名・コネクタ名は改名に追随しておらず `notebooklm` のまま**なので、接続先を探すときは旧称で探すこと。本ドキュメントでは以降 `notebook_create` のようにツール名のみで表記する。

使うツール: `notebook_create` / `source_add` / `source_wait` / `source_rename` / `source_delete` / `chat_ask` / `notebook_delete`

## Step 1: 入力ファイルの特定

引数でパスが渡されていればそれを使う。渡されていない場合、または渡されたパスが存在しない場合は以下の手順で特定する。

1. 録音の保存先ディレクトリをユーザーに尋ねる（会議ツールごとに既定の場所が異なる。既知のものは [references/tldv.md](references/tldv.md) を参照）。
2. そのディレクトリの候補を新しい順に列挙する。

```bash
dir="<録音ディレクトリ>"
find "$dir" -maxdepth 1 -type f \( -name '*.webm' -o -name '*.mp4' -o -name '*.m4a' -o -name '*.mp3' -o -name '*.wav' -o -name '*.mkv' \) -printf '%T@\t%p\n' \
  | sort -rn | head -20 | cut -f2- | while read -r f; do
      dur=$(ffprobe -v error -show_entries format=duration -of csv=p=0 "$f" 2>/dev/null)
      printf '%s | %5.1f分 | %s\n' \
        "$(date -r "$f" '+%Y-%m-%d %H:%M')" \
        "$(awk -v d="$dur" 'BEGIN{printf "%.1f", d/60}')" \
        "$(basename "$f")"
    done
```

表示される日時はファイルの更新日時（mtime）で、多くの録音ツールでは**録音終了時刻**とほぼ一致する。開始時刻を知りたい場合は mtime から長さを引く。ファイル名にタイムスタンプを埋め込むツールもあるので、規約が分かっている場合はそちらのほうが正確（[references/tldv.md](references/tldv.md) に例がある）。

### 自動選択してよい条件

**会議の想定時刻の前後3時間以内に候補がちょうど1件のときだけ自動採用する。** 以下のケースでは自動確定せず、上記一覧（日時・長さ・ファイル名）を提示してユーザーに選んでもらう。

- 3時間以内の候補が複数ある
- 3時間以内に候補が無い

採用前に**録音の長さが会議の想定時間と整合するか**を必ず確認する。数分しかないファイルは接続テストや誤録音の可能性が高い（実際、録音ディレクトリには 0〜1分の残骸が溜まりがち）。候補が1件も無い場合は別の場所に保存された可能性があるためユーザーに確認する。

### タイムゾーン変換の注意

ファイル名に UTC タイムスタンプが埋まっている場合、ローカル時刻への変換は `TZ=` 指定にもローカル TZ の暗黙変換にも頼らず、**epoch 秒に固定オフセットを足して `date -u` で出力する**。

```bash
# 例: UTC → JST（+9時間 = 32400秒）
ep=$(date -u -d "2026-09-13 01:23:45 UTC" +%s)
date -u -d "@$((ep + 32400))" '+%Y-%m-%d %H:%M'
```

Windows の Git Bash（MSYS2）には tzdata が無く `TZ='Asia/Tokyo'` が解決できずUTC扱いにフォールバックする。また、ローカル TZ が既に目的の TZ になっている環境で `date -d "... UTC +9 hours"` と書くと、加算とローカル TZ 変換が二重にかかって +18時間ずれる。どちらも**エラーにならず黙って間違う**ので、上記の epoch 方式で統一する。

## Step 2: 事前情報の収集と固有名詞リストの作成

**このステップを省略すると要約の質が大きく落ちる。** 文字起こしは固有名詞を高確率で誤るため、正しい表記を明示的に Gemini Notebook へ渡す必要がある。

収集するもの:

- 会議の目的・議題
- 参加者の氏名・所属・役割
- 関連資料（過去の同じ案件の議事録、企画書、相手先の情報など）
- 会議中に話題になりそうな製品名・サービス名・社内用語

これらから**固有名詞リスト**を作る。人名・会社名・製品名・サービス名・略語・数値の単位など、誤変換されると意味が変わるものをすべて拾う。

情報が手元に無い場合はユーザーに尋ねる。「参加者は誰か」「どの案件の会議か」の2点だけでも精度は目に見えて上がる。

> ノートアプリや課題管理システムと連携して事前情報を自動収集する場合は [references/obsidian.md](references/obsidian.md) を参照（Obsidian Vault との統合例）。

## Step 3: mp3 へ変換（必須）

**元の録音ファイルを Gemini Notebook に直接渡してはいけない。** `.webm` などは HTTP 400 で拒否される。さらに失敗したソースがゴーストとして残り、削除の手間が増える。

変換先は一時ディレクトリ（セッションのスクラッチパッド、無ければ `$TMPDIR` / `$TEMP`）。

```bash
ffmpeg -y -v error -i "<入力ファイル>" -vn -ac 1 -ar 16000 -b:a 64k "<出力.mp3>"
```

- `-vn` 映像を捨てる / `-ac 1` モノラル化 / `-ar 16000` 音声認識に十分な 16kHz / `-b:a 64k`
- 変換は音声長にほぼ比例し、mp3 は元ファイルの約半分のサイズになる。実測値は [references/tldv.md](references/tldv.md) のベンチマーク表を参照
- 変換後に `ffprobe` で長さが元と一致することを確認する
- ffmpeg が `command -v ffmpeg` で見つからない場合、Windows で WinGet 導入済みなら `$LOCALAPPDATA/Microsoft/WinGet/Links/ffmpeg` を試す。それでも無ければユーザーに報告して中断する

## Step 4: Gemini Notebook へアップロード

1. `notebook_create` で `title="YYYY-MM-DD <会議タイトル>"` の新規 Notebook を作成する（都度使い捨て）。

2. `source_add` を `source_type="file"`, `path=<変換後 mp3 の絶対パス>` で呼ぶ。

   MCP がリモート接続の場合、これは実バイトを送らず `status="upload_required"` と署名付き URL を返す。**`filename` 引数は使えない**（`bytes_base64` 専用でバリデーションエラーになる）。

3. 返ってきた `agent_upload.url` の `?filename=` を置き換えて curl で実バイトを POST する。

```bash
curl -sS -X POST -H "Accept: application/json" -H "Content-Type: audio/mpeg" \
  --data-binary @"<変換後 mp3 の絶対パス>" \
  "<agent_upload.url の ?filename= を meeting.mp3 にしたもの>"
```

   - **署名 URL の有効期限は約15分。** 変換を先に済ませてから `source_add` を呼ぶこと（逆順にすると長い変換中に失効しうる）
   - **`?filename=` の値がそのままソース名になる**（JWT に埋まったタイトルではない）。日本語名は URL エンコードが必要なので `meeting.mp3` のような ASCII 名で送り、必要なら後から `source_rename` で改名する
   - 成功すると `{"status":"added","source_id":"..."}` が返る
   - **失敗した場合はゴーストソースが残る。** レスポンスに出る source_id を `source_delete`（`confirm=true`）で消してから再試行する。消さずに再試行すると Notebook にエラー行が溜まる
   - curl 自体がネットワークエラーで失敗する場合のみ、`human_upload.url` をユーザーに提示してブラウザからのアップロードを依頼する

4. `source_wait` で `timeout=600`, `interval=10` 程度を指定して処理完了を待つ。

5. `source_add` を `source_type="text"`, `title="事前情報"` で呼び、Step 2 で集めた背景情報を渡す。**固有名詞リストを必ず含める。**

## Step 5: 要約質問

`chat_ask` に質問を投げる。プロンプトには以下を必ず含める。

- 会議の目的・参加者（事前情報から）
- **固有名詞の正しい表記リストを提示し、文字起こし側の誤認識をこの表記で補正するよう明示的に指示する**
- 求める出力構成: アジェンダ / 議事内容（トピックごと） / 決定事項 / ネクストアクション / 未確認・要フォロー事項
- 「聞き取れない・不確かな部分は断定せず『不明瞭』と明記する」よう指示

必要に応じて追加の `chat_ask`（`history` 付き）でトピックを深掘りする。

## Step 6: ドラフト提示（ユーザーレビュー必須）

- **Gemini Notebook の回答をそのまま保存しない。** まずチャット上にドラフトを提示し、ユーザーの確認・修正を受ける
- 情報の粒度に注意し、書きすぎない。論点に直接関係しない枝葉は入れない
- 裏が取れない固有名詞・数値は断定せず、ユーザーに確認する
- ユーザーから修正指示があれば反映し、確定するまで繰り返す

## Step 7: 保存

確定したドラフトを Markdown ファイルとして保存する。保存先が指定されていなければユーザーに尋ねる。

frontmatter を付ける場合の推奨項目:

```yaml
---
date: YYYY-MM-DD      # 会議実施日
title: <会議タイトル>
participants: [ ... ]  # 参加者
tags: [meeting]
---
```

ノートアプリ側の命名規約・タグ規約があればそれに合わせる。

## Step 8: 後片付け

- 変換した一時 mp3 を削除する
- 使い捨て Notebook を削除するかユーザーに確認する（`notebook_delete` は `confirm=true` が必要）。**既定は残す**（要約の裏取りに再利用できるため）。ユーザーが希望した場合のみ削除する

## 横断的な注意

### 日本語の固有名詞・専門用語は信用しない

Gemini Notebook の文字起こしは、会話の流れや数値（人数・日数など）は概ね正確に取れるが、**日本語の固有名詞と同音異義語の誤認識がかなり多い**。実際に観測した誤りの例:

| 実際 | 文字起こし結果 |
| --- | --- |
| 受託開発 | 自宅開発 |
| スカウト | スカート |
| 選考期間 | 商用期間 |
| 一次（選考） | 一時 |
| 承認制 | 証明性 |
| 週3 | 修産 |
| Claude Code | マックロードコード |
| Gemini | ジミニ |

社名・サービス名のようなカタカナ・アルファベットの固有名詞は、**原形をとどめない全く別の語に化けることがある**（日本語として意味の通る別の単語になるため、読んでも誤りだと気づきにくい）。だからこそ Step 2 の固有名詞リストを省略しないこと。Gemini Notebook の回答は必ず事前情報と突き合わせ、裏が取れない固有名詞は断定せずユーザーに確認する。

### 録音長には意味の異なる2つの値がある

録音ツールによっては、**プレーヤーが表示する長さと `ffprobe` の `format=duration` が一致しない**。

| 指標 | 取得方法 | 意味 |
| --- | --- | --- |
| **実音声の長さ** | パケットの `duration_time` の総和 | 実際に収録されている音声の合計。**プレーヤーが表示するのはこちら** |
| **タイムラインの長さ** | `ffprobe -show_entries format=duration` | 録音の実時間（mtime − 録音開始時刻と一致する） |

録音側が周期的に音声を取りこぼしていると、その分だけ実音声がタイムラインより短くなる（tl;dv での実測では約3.8%の欠落があった。詳細は [references/tldv.md](references/tldv.md)）。

**ユーザーに録音長を伝えるときは実音声の長さを使う。** `format=duration` をそのまま「録音は◯分」と報告するとプレーヤー表示と食い違う。一方、会議の予定時間と突き合わせて候補を絞る用途（Step 1）ではタイムライン長のほうが適切。

実音声の長さを出すワンライナー:

```bash
ffprobe -v error -select_streams a -show_entries packet=duration_time -of csv=p=0 "$f" \
  | awk '{s+=$1} END {printf "%.2f s (%d分%05.2f秒)\n", s, int(s/60), s-60*int(s/60)}'
```

なお変換後の mp3 はタイムライン長になる。ffmpeg が欠落箇所に無音を埋めるためで、処理上の問題はない。

### モデルの固定について

このスキルは frontmatter で **Opus / effort:high に固定**している。文字起こしの固有名詞誤変換の復元とファクトチェックが成果物の精度に直結するため。

ただし Claude Code ではこの上書きが効くのは**スキルを起動したターンのみ**で、次のプロンプトからはセッションのモデルに戻る。Step 6 のドラフトレビュー往復も Opus で通したい場合は、あらかじめ `/model opus` でセッション自体を切り替えておくこと。

### 環境依存の絶対パスを直書きしない

複数の PC でこのスキルを使う場合、ユーザーホームの絶対パス（`C:\Users\<名前>\...`、`/home/<名前>/...`）を手順やスクリプトに直書きしない。`$HOME` / `$LOCALAPPDATA` / `$TMPDIR` などの環境変数経由で参照する。

## 注意事項まとめ

- **ffmpeg 変換は必須。** 元の録音ファイルをそのまま投げてはいけない
- ファイル・候補が曖昧な場合は絶対に自動選択せず、必ずユーザーに確認する
- 文字起こしの固有名詞は信用せず、必ず事前情報で裏を取る
- 署名付きアップロード URL は約15分で失効する
- アップロード失敗時はゴーストソースを削除してから再試行する
- Gemini Notebook の回答をそのまま保存せず、必ずドラフトレビューを挟む
- タイムゾーン変換は epoch 秒 + 固定オフセット方式を使う

## 参考資料

- [references/tldv.md](references/tldv.md) — tl;dv クライアントのローカル録音に関する実測知見（ファイル名規約・音声欠落の調査・処理時間ベンチマーク）
- [references/obsidian.md](references/obsidian.md) — Obsidian Vault と統合する場合のオプション手順
